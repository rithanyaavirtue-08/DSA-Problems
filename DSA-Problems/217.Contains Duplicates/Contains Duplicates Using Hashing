class Solution {
    public boolean containsDuplicate(int[] nums) {
        int[] h = new int[100001];

        for (int i = 0; i < nums.length; i++) {

            h[nums[i]]++;
            if (h[nums[i]] >= 2) {
                return true;
            }

        }

        return false;

    }
}
